# Test-Project-Cadence

Test repository for BLD-04 OpenHands delegation end-to-end validation.

Cadence delegates build tasks here via the `delegate.openhands` tool.
OpenHands clones this repo, implements the task, and opens a PR.
